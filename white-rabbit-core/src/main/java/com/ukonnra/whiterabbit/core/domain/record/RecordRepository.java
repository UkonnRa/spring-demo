package com.ukonnra.whiterabbit.core.domain.record;

import com.ukonnra.whiterabbit.core.AbstractRepository;
import org.springframework.stereotype.Repository;

@Repository
public interface RecordRepository extends AbstractRepository<RecordEntity> {}
