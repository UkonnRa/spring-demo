type UserEntity {
  id: ID!
  version: Int!
  name: String!
  role: RoleValue!
  authIds: [AuthIdValue!]!
}

type AuthIdValue {
  provider: String!
  tokenValue: String!
}

enum RoleValue {
  USER
  ADMIN
  OWNER
}

type Query {
  user(id: ID): UserEntity!
}
